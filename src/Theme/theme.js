import { createTheme } from '@mui/material';
export const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(26 24 23 / 70%)',
    },
    secondary: {
      main: '#ffff',
    },
    backgroundColor: {
      main: 'transparent',
    },
  },
});
